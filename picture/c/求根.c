#include<stdio.h>
#include<math.h>
int main()
{
  double a,b,c,q,p,disco,x1,x2;
  scanf("%f,%f,%f",&a,&b,&b);
  disco=sqrt(b*b-4*a*c);
  q=(sqrt(disco))/2*a;
  p=-b/2*a;
  printf("x1=%f,x2=%f",p+q,p-q);
  return 0;
}
